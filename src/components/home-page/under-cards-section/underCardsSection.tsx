import { Container } from '@mui/material';
import styles from './under-cards-section.module.css';

const UnderCardsSection = () => {
  return (
    <Container className="mt-4 max-w-md-65">
      <div className={styles.mainDivStyle}>
        
      </div>
    </Container>
  );
};

export default UnderCardsSection;
